settings:
  category_name: "debrid"
  save_path: "/mnt/zurg/__all__"
  validate_paths: true

  real_debrid:
    token: "${REAL_DEBRID_TOKEN}"

  sonarr:
    host: "${SONARR_HOST}"
    token: "${SONARR_TOKEN}"

  radarr:
    host: "${RADARR_HOST}"
    token: "${RADARR_TOKEN}"

  zurg:
    host: "${ZURG_HOST}"
